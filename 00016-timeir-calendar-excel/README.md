# Time IR Events to Excel

دریافت رویداد ها و مناسبت های تقویم به صورت فایل اکسل و قابل استفاده برای برنامه نویسی

## Requirements

```cmd
pip install -U pyqt5 pandas selenium selenium-wire webdriver-manager
```
